const defaultCellSize = 12;
const zoomStep = 0.1;

const styleContainer = {
                        'height': "84vh",
                        'overflowX': "auto",
                        'overflowY': "auto"
                      };

const emptyFunction = () => {};

export { defaultCellSize, zoomStep, emptyFunction, styleContainer };